import { IfEqualElsePipe } from './if-equal-else.pipe';

describe('IfEqualElsePipe', () => {
  it('create an instance', () => {
    const pipe = new IfEqualElsePipe();
    expect(pipe).toBeTruthy();
  });
});
