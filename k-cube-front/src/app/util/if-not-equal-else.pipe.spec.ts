import { IfNotEqualElsePipe } from './if-not-equal-else.pipe';

describe('IfNotEqualElsePipe', () => {
  it('create an instance', () => {
    const pipe = new IfNotEqualElsePipe();
    expect(pipe).toBeTruthy();
  });
});
